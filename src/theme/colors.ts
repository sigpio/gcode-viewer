/**
 * Centralized color palette for the application
 * 
 * All colors are defined in tailwind.config.cjs under theme.extend.colors
 * and are available as Tailwind CSS utility classes (e.g., bg-toolpath-extrusion)
 * 
 * Note: For THREE.js colors, use getThreeColors() from getThreeColors.ts instead.
 * This reads colors from CSS variables, which updates dynamically with theme changes.
 */

// This file is currently empty as all colors are managed through:
// 1. Tailwind config (UI colors)
// 2. CSS variables in index.css (Three.js colors)
// 3. getThreeColors() utility function (Three.js color access)
